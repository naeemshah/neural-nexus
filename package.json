{
  "name": "neural-nexus",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "crypto"
  ],
  "scripts": {
    "test:crypto": "cd crypto && npx hardhat test",
    "compile:crypto": "cd crypto && npx hardhat compile"
  }
}
